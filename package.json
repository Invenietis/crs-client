{
  "name": "crs-client",
  "version": "2.0.0-alpha.0",
  "main": "src/index.js",
  "description": "",
  "directories": {
    "test": "test"
  },
  "exclude": [
    "bundling"
  ],
  "scripts": {
    "tsc": "tsc",
    "bundle": "cross-env EXCLUDE_RX=1 webpack --profile --bail",
    "bundle:observable": "webpack --profile --bail",
    "build": "npm run tsc && npm run bundle && npm run bundle:observable"
  },
  "peerDependencies": {
    "rxjs": "^5.0.0"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^3.5.0",
    "cross-env": "^5.0.1",
    "rxjs": "^5.4.2",
    "awesome-typescript-loader": "^3.2.1",
    "typescript": "~2.4.1",
    "webpack": "~3.1.0"
  },
  "author": "",
  "license": "ISC"
}
